#include <stdio.h>

int main() {

    // making variables
    int number = 0;

    // while loop
    while (number < 5) {
        printf("Number is %d\n", ++number);
    }
        
    return 0;
}